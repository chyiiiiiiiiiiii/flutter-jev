Gemini model: gemini-3.1-flash-lite · 12 turns · 3 repeats

| mode | agrees with designer | median latency | p90 | median output tokens | median total tokens |
|---|---|---|---|---|---|
| generate | 27/36 | 1369ms | 1792ms | 218 | 916 |
| select | 30/36 | 770ms | 884ms | 3 | 325 |
| jev | 25/36 | 225ms | 257ms | 99 | 1122 |

Where the modes disagree with the designer's expectation:

- `generate` on "where is my refund" → `plain_reply` (expected `refund_status`)
- `jev` on "where is my refund" → `order_list` (expected `refund_status`)
- `generate` on "my parcel hasn't moved in four days, I need help" → `order_detail` (expected `contact_form`)
- `select` on "my parcel hasn't moved in four days, I need help" → `tracking_timeline` (expected `contact_form`)
- `jev` on "my parcel hasn't moved in four days, I need help" → `tracking_timeline` (expected `contact_form`)
- `generate` on "I got the wrong size" → `order_list` (expected `return_form`)
- `select` on "I got the wrong size" → `order_list` (expected `return_form`)
- `jev` on "I got the wrong size" → `order_list` (expected `return_form`)
- `jev` on "what's in my cart" → `plain_reply` (expected `cart_summary`)
